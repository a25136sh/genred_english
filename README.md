# FastAPI Application

FastAPI application with Nginx proxy and MySQL database.

## Setup

```bash
docker compose up --build
```

## Endpoints

- `GET /` - Hello World
- `GET /problems` - Get latest 30 problems
